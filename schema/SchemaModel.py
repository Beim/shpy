
class SchemaModel:

    def __init__(self, primary_label, extends, entity_schema):
        self.primary_label = primary_label
        self.extends = extends
        self.entity_schema = entity_schema
